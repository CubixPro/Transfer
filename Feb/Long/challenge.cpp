#include<bits/stdc++.h>

using namespace std;

int main(){
    int n, m, r;
    scanf("%d %d %d", &n, &m, &r);
    for(int i = 0 ; i < r ; i++){
        int x, y;
        scanf("%d %d", &x, &y);
        cin >> x >> y;
    }
    cout << "1 1 0";
}